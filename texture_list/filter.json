{
    "description": "Automatically creates the `texture_list.json` file, based on the images you've added into your resource pack.",
    "filters": [
        {
            "runWith": "python",
            "script": "./texture_list.py",
            "name": "Generating texture_list.json"
        }
    ]
}